extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      //- Title 
      h1 My Portfolio

      //- Portfolio description 
      p.lead.text-light
        | This is a portfolio that demonstrates my skills as a web developer. This project itself is an Express site utilizing Node.js to showcase my other projects using client-side Javascript.
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3


      each proj in projects
        .cell
          a(href=`/projects/${proj.id}`)
            img.thumbnail(src=`${proj.image_urls[0]}`)
            h5 #{proj.project_name}
